# ReplicationController & ReplicaSet

Use ReplicaSet to replace ReplicationController, as they are almost the same,
but ReplicaSet supports set selector. Deployments is more higher resource that
use ReplicaSet to management container such as replicas, roll update, rollout,
version tag.
